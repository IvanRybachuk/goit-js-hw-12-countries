<h2>{{name}}</h2>
<ul>
    <li>Capital: {{capital}}</li>
    <li>Population: {{population}}</li>
    <li>Languages: 
        <ul>
            {{#each languages}}
            <li>{{this.name}}</li>
            {{/each}}
        </ul>
    </li>
</ul>

<img src="{{flag}}" alt="">